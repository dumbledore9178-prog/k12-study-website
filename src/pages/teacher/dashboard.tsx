import Head from 'next/head';\nimport { useSession } from 'next-auth/react';\nimport { useRouter } from 'next/router';\nimport { useEffect, useState } from 'react';\nimport { MdAssignment, MdPeople, MdViewAgenda } from 'react-icons/md';\n\nexport default function TeacherDashboard() {\n  const { data: session, status } = useSession();\n  const router = useRouter();\n  const [classrooms, setClassrooms] = useState([]);\n  const [selectedClassroom, setSelectedClassroom] = useState<any>(null);\n\n  useEffect(() => {\n    if (status === 'unauthenticated') {\n      router.push('/auth/login');\n    }\n  }, [status, router]);\n\n  if (status === 'loading') {\n    return <div className=\"flex items-center justify-center h-screen\">Loading...</div>;\n  }\n\n  if (session?.user?.role !== 'TEACHER') {\n    return (\n      <div className=\"flex items-center justify-center h-screen\">\n        <p className=\"text-red-600\">Access denied. Teacher account required.</p>\n      </div>\n    );\n  }\n\n  return (\n    <>\n      <Head>\n        <title>Teacher Dashboard - K12 Study</title>\n      </Head>\n\n      <main className=\"p-6 bg-gray-50 min-h-screen\">\n        <div className=\"max-w-6xl mx-auto\">\n          <h1 className=\"text-4xl font-bold text-gray-900 mb-8\">👨‍🏫 Teacher Dashboard</h1>\n\n          {/* Quick Actions */}\n          <div className=\"grid grid-cols-1 md:grid-cols-3 gap-6 mb-8\">\n            <button className=\"bg-white rounded-lg shadow p-6 hover:shadow-lg transition text-center\">\n              <MdAssignment size={40} className=\"mx-auto mb-3 text-primary-600\" />\n              <h3 className=\"font-semibold text-gray-900\">Assign Skills</h3>\n              <p className=\"text-sm text-gray-600 mt-1\">Create assignments for your classes</p>\n            </button>\n            <button className=\"bg-white rounded-lg shadow p-6 hover:shadow-lg transition text-center\">\n              <MdPeople size={40} className=\"mx-auto mb-3 text-green-600\" />\n              <h3 className=\"font-semibold text-gray-900\">View Students</h3>\n              <p className=\"text-sm text-gray-600 mt-1\">Monitor student progress</p>\n            </button>\n            <button className=\"bg-white rounded-lg shadow p-6 hover:shadow-lg transition text-center\">\n              <MdViewAgenda size={40} className=\"mx-auto mb-3 text-blue-600\" />\n              <h3 className=\"font-semibold text-gray-900\">Live Screen View</h3>\n              <p className=\"text-sm text-gray-600 mt-1\">See all student screens</p>\n            </button>\n          </div>\n\n          {/* Classrooms */}\n          <div className=\"bg-white rounded-lg shadow p-6 mb-8\">\n            <h2 className=\"text-2xl font-bold text-gray-900 mb-4\">My Classrooms</h2>\n            <div className=\"space-y-3\">\n              {[\n                { name: 'Math - Grade 4A', students: 28, joined: '15/28' },\n                { name: 'ELA - Grade 5B', students: 25, joined: '24/25' },\n                { name: 'Science - Grade 6', students: 30, joined: '30/30' },\n              ].map((classroom, i) => (\n                <div\n                  key={i}\n                  onClick={() => setSelectedClassroom(classroom)}\n                  className=\"p-4 border border-gray-200 rounded-lg hover:bg-gray-50 cursor-pointer transition\"\n                >\n                  <div className=\"flex justify-between items-center\">\n                    <div>\n                      <h3 className=\"font-semibold text-gray-900\">{classroom.name}</h3>\n                      <p className=\"text-sm text-gray-600 mt-1\">{classroom.joined} students active</p>\n                    </div>\n                    <span className=\"text-lg font-bold text-primary-600\">{classroom.students}</span>\n                  </div>\n                </div>\n              ))}\n            </div>\n          </div>\n\n          {/* Selected Classroom Details */}\n          {selectedClassroom && (\n            <div className=\"bg-white rounded-lg shadow p-6\">\n              <h2 className=\"text-2xl font-bold text-gray-900 mb-4\">{selectedClassroom.name}</h2>\n              <div className=\"grid grid-cols-1 lg:grid-cols-3 gap-6\">\n                {/* Student List */}\n                <div className=\"lg:col-span-2\">\n                  <h3 className=\"font-semibold text-gray-900 mb-3\">Students</h3>\n                  <div className=\"space-y-2 max-h-96 overflow-y-auto\">\n                    {[1, 2, 3, 4, 5].map((student) => (\n                      <div key={student} className=\"flex items-center gap-3 p-2 hover:bg-gray-50 rounded\">\n                        <div className=\"w-8 h-8 bg-primary-200 rounded-full\"></div>\n                        <div className=\"flex-1\">\n                          <p className=\"text-sm font-medium text-gray-900\">Student {student}</p>\n                          <p className=\"text-xs text-gray-600\">Currently: Fractions (85%)</p>\n                        </div>\n                        <span className=\"text-xs font-semibold text-green-600\">Online</span>\n                      </div>\n                    ))}\n                  </div>\n                </div>\n\n                {/* Assignments */}\n                <div>\n                  <h3 className=\"font-semibold text-gray-900 mb-3\">Active Assignments</h3>\n                  <div className=\"space-y-2\">\n                    {[1, 2].map((assign) => (\n                      <div key={assign} className=\"p-3 bg-blue-50 rounded-lg\">\n                        <p className=\"text-sm font-medium text-gray-900\">Assignment {assign}</p>\n                        <p className=\"text-xs text-gray-600 mt-1\">15/28 completed</p>\n                      </div>\n                    ))}\n                  </div>\n                </div>\n              </div>\n            </div>\n          )}\n        </div>\n      </main>\n    </>\n  );\n}
